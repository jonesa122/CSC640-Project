+++
title = "Run API Test Script"
description = "Testing endpoints with the provided shell script"
weight = 3
+++
# 🧪 Run API Test Script

To test all endpoints in the CSC640 project, use the provided shell script:

### `test-api.sh`

It covers:
- User registration and login
- Animal listing, creation, update, deletion
- Adoption submission and updates

## 🚀 How to Run It

1. Open your terminal in the project folder  
2. Make the script executable:

```bash
chmod +x test-api.sh
```
3. Run the script:
```bash
./test-api.sh
```